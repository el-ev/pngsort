{
  "name": "pngsort-cloudflare-pages",
  "version": "1.0.0",
  "description": "PNG Sort Cloudflare Pages Application",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "wrangler pages deploy dist"
  },
  "dependencies": {
    "@types/node": "^20.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "wrangler": "^3.114.14"
  }
}
